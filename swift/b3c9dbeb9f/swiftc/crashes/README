Criteria to confirm it is a crash: special commands lead to crash, while driver does not capture that (corner cases are "import")

# swiftc -parse $FILE

# swiftc -ast-dump $FILE
read_Types.h:2875_1.swift
read_Types.h:2875_2.swift

# swiftc -emit-bc $FILE
